Sample documentation for smoke testing the DSAGT knowledge base.
Covers the "greet" CLI fixture (tests/smoke_test/greet.py) — the same
utility the smoke script has the agent register and execute, so the
ingested docs, the registered code, and the provenance records all
describe one consistent tool.

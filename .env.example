# Copy to .env and fill in values to run the integration test suite
# (tests/test_*_integration.py, deselected by default via `-m "not integration"`).
# See tests/conftest.py for how these are loaded and validated.

EMBEDDING_BASE_URL=
EMBEDDING_API_KEY=
EMBEDDING_MODEL=

LLM_PROVIDER=
LLM_MODEL=
LLM_BASE_URL=
LLM_API_KEY=
